# This can also be done as an oh-my-zsh plugin, but requires doing things by hand
# Here we just source the packaged version so it stays updated with the rest of the system

if [[ -f /usr/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh ]]; then
    source /usr/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh
fi
